management:
  endpoint:
    health:
      probes:
        enabled: true

spring:
  data:
    web:
      pageable:
        default-page-size: 10
  flyway:
    enabled: true
    locations: classpath*:db/migration
  mvc:
    problemdetails:
      enabled: true

springdoc:
  swagger-ui:
    path: '/'