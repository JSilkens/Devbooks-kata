management:
  endpoint:
    health:
      probes:
        enabled: true

spring:
  flyway:
    enabled: true
    locations: classpath*:db/migration
  mvc:
    problemdetails:
      enabled: true

springdoc:
  swagger-ui:
    path: '/'